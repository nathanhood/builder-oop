button.button.button-action#plant Plant
button.button.button-warning#getForest Get Forest
#user(data-id=user._id.toString())= user.username
.wood Wood:
  #wood #{user.wood.toFixed(0)} pc
.cash Cash:
  #cash $#{user.cash.toFixed(2)}
form
  input.woodqty(type='text', name='woodQty', placeholder='Qty')
  button.button.button-action#sell Sell
if(user.isAutoGrowAvailable)
  button.button.button-caution#purchase-autogrow Auto Grow
#achievements
  each item in user.items
    case item.type
      when 'autogrow'
        include ../items/autogrow
